---
name: Runtime bug
about: The brainstem runs but misbehaves (chat, agents, UI, auth)
labels: bug
---

**What you did, what you expected, what happened instead**:

**Your `/health` right now** (paste the output of):

```bash
curl -s localhost:7071/health
```

**OS + install age** (fresh install or long-running? roughly which version did you start on?):

**Server log tail around the failure**:

```
```
